package asgn.astro.console.models

data class AstroListModel(var listID: Long = 0,
                          var list: String = "")

//var list = to be replaced with an actual List later rather than a String
